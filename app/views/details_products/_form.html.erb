<%= form_for(@details_product) do |f| %>
  <% if @details_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@details_product.errors.count, "error") %> prohibited this details_product from being saved:</h2>

      <ul>
      <% @details_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="field">
    <%= f.label :type_product_id %><br>
    <%= f.number_field :type_product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
