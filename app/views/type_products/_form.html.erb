<%= form_for(@type_product) do |f| %>
  <% if @type_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_product.errors.count, "error") %> prohibited this type_product from being saved:</h2>

      <ul>
      <% @type_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :list_product_id %><br>
    <%= select(:type_product, :list_product_id, options_from_collection_for_select(@list_products, :id, :name)) %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
